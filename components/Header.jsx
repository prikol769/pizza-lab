const Header = () => {
  const style = {
    color: "red",
    fontSize: "48px",
  };
  return (
    <header className="header">
      <h1 style={style}>Fast React Pizza</h1>
    </header>
  );
};

export default Header;
